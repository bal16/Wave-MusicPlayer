[project]
name = "wave-musicplayer"
version = "0.1.0"
description = "Wave Music Player — local music player"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "customtkinter>=6.0.0",
    "loguru>=0.7.3",
    "miniaudio>=1.71",
    "pillow>=12.3.0",
    "python-vlc>=3.0.21203",
    "sqlmodel>=0.0.42",
    "tinytag>=2.3.1",
]

[tool.uv]
managed = true

[dependency-groups]
dev = [
    "poethepoet>=0.48.0",
    "pyinstaller>=6.22.2",
    "pyinstaller-hooks-contrib>=2026.7",
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]

[tool.poe.tasks]
start        = { cmd = "python main.py", help = "Run the app" }
dev          = { cmd = "python main.py", help = "Run the app with debug logging", env = { LOGURU_LEVEL = "DEBUG" } }
check        = { cmd = "python -m compileall -q main.py view.py components domain infrastructure services app controllers views utils", help = "Byte-compile all first-party modules (no display needed)" }
lint         = { cmd = "ruff check tests/ utils/ domain/ infrastructure/ services/ app/ controllers/ views/", help = "Lint layered + tooling code (legacy UI files excluded)" }
format       = { cmd = "ruff format tests/ utils/ domain/ infrastructure/ services/ app/ controllers/ views/", help = "Format layered + tooling code" }
format-check = { cmd = "ruff format --check tests/ utils/ domain/ infrastructure/ services/ app/ controllers/ views/", help = "Fail if layered/tooling files are unformatted" }
test         = { cmd = "pytest", help = "Run test suite" }
quality      = { sequence = [{ ref = "check" }, { ref = "lint" }, { ref = "format-check" }, { ref = "test" }], help = "Full quality gate" }
build        = { sequence = [{ ref = "check" }, { ref = "build-exe" }], help = "Check + build onedir bundle into dist/Wave/" }
build-exe    = { cmd = "pyinstaller wave.spec --noconfirm", help = "Build onedir bundle via wave.spec" }
dist         = { cmd = "pyinstaller wave-onefile.spec --noconfirm", help = "Build single-file binary into dist/Wave" }
clean        = { cmd = "python -c \"import shutil; [shutil.rmtree(d, ignore_errors=True) for d in ['build', 'dist']]\"", help = "Remove build/ and dist/" }

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["__old", ".venv", "build", "dist"]

[tool.ruff.lint]
select = ["F", "E", "W", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q"
